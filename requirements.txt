# Dépendances du dépôt, par usage.
#
# Le socle `btcterm/` n'a besoin que du premier bloc ; le terminal et les
# outils en ligne de commande ajoutent les leurs. Installer tout le
# fichier fait tourner l'ensemble :
#     pip install -r requirements.txt

# ── Socle (btcterm) ─────────────────────────────────────────
pandas>=2.0
numpy>=1.24
requests>=2.31.0

# ── Terminal web (terminal/) ────────────────────────────────
dash>=2.14
plotly>=5.18

# ── Flux temps réel (carnets, arbitrage) ────────────────────
websockets>=12.0
rich>=13.0

# ── Flux ETF (etf_bitcoin_flows.py) ─────────────────────────
lxml>=5.0
beautifulsoup4>=4.12
tabulate>=0.9

# ── News (news/btc_news.py) ─────────────────────────────────
feedparser>=6.0.11
