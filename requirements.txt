# Dépendances du dépôt, par usage.
#
# Le socle `btcterm/` n'a besoin que du premier bloc ; chaque panneau
# ajoute les siennes. Installer tout le fichier fait tourner l'ensemble
# des outils :
#     pip install -r requirements.txt

# ── Socle (btcterm) ─────────────────────────────────────────
pandas>=2.0
numpy>=1.24
requests>=2.31.0

# ── Dashboards web (btc-dash.py, btc_dashboard2.py) ─────────
dash>=2.14
dash-bootstrap-components>=1.5
plotly>=5.18
ccxt>=4.2

# ── Fenêtres matplotlib (btc-liquidity.py, btc_orderbook_live.py) ──
matplotlib>=3.8

# ── Flux temps réel (carnets, arbitrage) ────────────────────
websockets>=12.0
rich>=13.0

# ── Flux ETF (etf_bitcoin_flows.py, etf.py) ─────────────────
lxml>=5.0
beautifulsoup4>=4.12
tabulate>=0.9

# ── News (news/btc_news.py) ─────────────────────────────────
feedparser>=6.0.11
