"""
Terminal Bitcoin — couche de rendu web.

Une application Dash unique regroupant tous les panneaux, servie en local
et accessible à distance par tunnel SSH :

    ssh -L 8050:localhost:8050 <machine>

Le socle `btcterm` fournit les données ; ce paquet ne fait que les
afficher.
"""
